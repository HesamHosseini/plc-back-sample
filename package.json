{
  "name": "json-server-custom",
  "version": "1.0.0",
  "description": "Custom JSON Server with _id support",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --id _id"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
